use proconio::input;
use std::{thread, time};

fn 

fn main() {
    let low = time::Instant::now();
    input! { 
        mut si: usize, 
        mut sj: usize, 
        t: [[usize; 50]; 50], 
        p: [[usize; 50]; 50], 
    }

    let mut ans = String::new();

    let mut visited = vec![false; 2500];
    
}